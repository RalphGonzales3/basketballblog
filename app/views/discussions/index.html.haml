%h1 Discussions

%table
  %thead
    %tr
      %th Thread
      %th Basketball
      %th{:colspan => "3"}

  %tbody
    - @discussions.each_with_index do |discussion, i|
      - row_class = ""
      - if i % 2 == 0
        - row_class = "even"
      -else
        - row_class = "odd"

      %tr
        %td{:class => "#{row_class}"}= discussion.body
        %td{:class => "#{row_class}"}= discussion.basketball.name
        %td{:class => "#{row_class}"}= link_to 'Show Player', [@basketball]
        %td{:class => "#{row_class}"}= link_to 'Edit Post', edit_basketball_discussion_path(@basketball, discussion)
        %td{:class => "#{row_class}"}= link_to 'Destroy Post', [@basketball, discussion], method: :delete, data: { confirm: 'Are you sure?' }



%br

= link_to 'New Discussion', new_basketball_discussion_path(@basketball)
